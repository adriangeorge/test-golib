package testgolib

import "strings"

func Replace(s string) string {
	return strings.Replace(s, "Hello", "Goodbye", -1)
}
func Replace2(s string) string {
	return strings.Replace(s, "Hello", "Goodbye", -1)
}
